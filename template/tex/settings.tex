% Settings/flags do template

% Se true, usa minted (requer `-shell-escape` + pygments instalado)
\newif\ifuseminted
\usemintedfalse

% Logo (se existir em img/logo.png será usado automaticamente na capa)
% \newcommand{\ReportLogoPath}{img/logo.png}
